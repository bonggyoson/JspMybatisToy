<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.JspMybatisSample.mapper.member.MemberMapper">

  <select id="selectMembers" resultMap="memberDto">
    SELECT *
    FROM board.member
    ORDER BY member_id
  </select>

  <select id="selectMember" resultMap="memberDto">
    SELECT *
    FROM board.member
    WHERE member_email = #{memberEmail}
    ORDER BY member_id
  </select>

  <insert id="insertMember"
    parameterType="com.example.JspMybatisSample.domain.member.dto.InsertMemberDto">
    <selectKey keyProperty="see_member" resultType="java.lang.Long" order="BEFORE">
      SELECT seq_member.NEXTVAL FROM DUAL
    </selectKey>
    <![CDATA[
        INSERT INTO board.member
        (
            member_id,
            member_email,
            member_password,
            member_role,
            created_at,
            updated_at
        )
            VALUES
        (
            #{seq_member},
            #{memberEmail},
            #{memberPassword},
            #{memberRole},
            now(),
            now()
        )
    ]]>
  </insert>

</mapper>